$('.answer-errors').html('<%= render 'errors', resource: @answer %>');

<% if @answer.persisted? %>
  $('.answers').append('<%= j render @answer %>');
  $('.new-answer #answer_body').val('');
  $('.notice').html('<%= notice %>');
<% end %>