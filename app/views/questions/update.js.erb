$('.question-errors').html('<%= render 'errors', resource: @question %>');

<% unless @question.errors.present? %>
  $('.question').html('<%= j render 'question_full', question: @question %>');
<% end %>